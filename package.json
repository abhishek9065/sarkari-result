{
  "name": "sarkari-result",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm ci && npm run build",
    "dev": "cd client && npm start"
  },
  "workspaces": [
    "client",
    "server"
  ]
}
