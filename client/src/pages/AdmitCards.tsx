import React from 'react';
import { Container, Typography } from '@mui/material';

const AdmitCards: React.FC = () => {
  return (
    <Container sx={{ py: 4 }}>
      <Typography variant="h4">Admit Cards</Typography>
      <Typography>Admit cards page coming soon...</Typography>
    </Container>
  );
};

export default AdmitCards;
