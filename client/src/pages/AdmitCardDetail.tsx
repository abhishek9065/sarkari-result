import React from 'react';
import { Container, Typography } from '@mui/material';

const AdmitCardDetail: React.FC = () => {
  return (
    <Container sx={{ py: 4 }}>
      <Typography variant="h4">Admit Card Detail</Typography>
      <Typography>Admit card detail page coming soon...</Typography>
    </Container>
  );
};

export default AdmitCardDetail;
